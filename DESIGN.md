# 流光日程设计规范

本版本按 UI/UX Pro Max 的 Glassmorphism 检索结果设计，保留原项目的 WPF / .NET Framework 技术栈。玻璃拟态的主要手段是半透明分层、柔光、细边框与颗粒；并未把检索结果中面向网页的 `backdrop-filter` 或高成本的持续模糊直接搬进 WPF。内容面板更不透明，以维持浅色和深色下的文字对比度。

## 视觉层级

- 窗口：28px 单层圆角，浅／深渐变底色，两处低强度径向柔光和轻颗粒。
- 导航：玻璃轨道与独立选中底片；内容区是第二层玻璃，任务卡是第三层。
- 弹层：编辑、详情与设置面板使用更高不透明度，并在内容区加柔和遮罩，避免背后的字透出干扰阅读。
- 操作：海蓝主题色 `#286C91`，按钮使用同色系轻渐变；用户可在设置中改为其他颜色。
- 文字：主文字、次文字和辅助文字分别使用语义资源；不把弱灰文字直接放在完全透明背景上。
- 默认玻璃浓度为 80%；自定义主题色的渐变端点会自动校正，保证按钮文字与背景至少达到 4.5:1。

## 动效取舍

| 位置 | 用途 | 实现 |
| --- | --- | --- |
| 玻璃浓度数字 | 状态反馈 | 新值即时显示，数字以 4px 位移与透明度在 150ms 内归位；快速连续拖动时优先可读性。 |
| 滑杆拇指 | 拖动反馈 | 拖动时缩放到 1.13，结束后 150ms 复原，不改布局。 |
| 视图标签 | 当前状态 | 选中底片 250ms 平移，首次布局不播放过渡。 |
| 主题色区域 | 展开状态 | 250ms 进入、150ms 退出；可被再次点击中断。 |

任务卡悬停与日历日期文字不增加位移动效，避免频繁使用时的跳动。系统关闭动画时，以上动效直接落到最终状态。所有动效只操作独立视觉层的变换或透明度；标签底片宽度变化局限于不参与周围排版的底片。

## 材质限制

当前方案是真实半透明的 WPF 材质，但不实时模糊窗口后方的桌面或应用。这样可维持四角裁剪一致，并避免重新引入系统 DWM 背景与 WPF 圆角不一致的问题。
